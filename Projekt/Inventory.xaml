<Window x:Class="Projekt.Inventory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projekt"
        mc:Ignorable="d"
        Title="Inventory" Height="450" Width="450"
         Opacity=".75"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True"
        WindowStyle="None"
        KeyDown="Window_KeyDown">
    <Grid Background="Gray" >
        <ListView Background="Transparent" x:Name="InvList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Height="50" Width="430" Background="DarkCyan" Opacity="1" >
                        <Image Source="{Binding Image}" Height="50" Width="50" ></Image>
                        <TextBlock Text="{Binding Name}" Width="320" TextAlignment="Center" FontSize="30"></TextBlock>
                        <TextBlock Text="{Binding Amount}" Width="50" TextAlignment="Center" FontSize="40"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
